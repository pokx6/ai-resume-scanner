# ai-resume-scanner
This AI uses a Transformer model to map resumes into a high-dimensional vector space. It leverages pre-trained knowledge from billions of web sentences to understand professional context. By calculating Cosine Similarity, it matches candidates to roles based on semantic intent.
